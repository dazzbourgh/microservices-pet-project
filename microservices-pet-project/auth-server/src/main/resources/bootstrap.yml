spring:
  cloud:
    config:
      name: auth-server
    discovery-service:
      id: auth-server
      enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/